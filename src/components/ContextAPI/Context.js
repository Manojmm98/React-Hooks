import React from 'react';
// we just create a variable of Mycontext by react.createcontext
const Mycontext= React.createContext('hello');
// now we export we just export my default context
export default Mycontext;